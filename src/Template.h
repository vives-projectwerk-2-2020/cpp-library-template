#pragma once    // Makes sure includes are only included once

namespace LibraryName
{
    class Template
    {
        public:
            // Conststructors
        
        public:
            // Public functions
            void exampleMethod();

        private:
            // Private functions
            // (functions only used internally)
        private:
            // Attributes
            // (only if you have a extremely good reason
            // should they be anything other than private)
    };
};